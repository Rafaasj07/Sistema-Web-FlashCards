const fs = require('fs');
const Pdf = require('./lib/pdf-parse.js');

module.exports = Pdf;

let isDebugMode = !module.parent;

//process.env.AUTO_KENT_DEBUG

if (process.env.AUTO_KENT_DEBUG) {
    isDebugMode = true;
}

/* 
if (isDebugMode) {
  //for testing
  let PDF_FILE = 'C:\\Users\\ResTIC16\\Desktop\\FlashCardsWeb\\API\\test\\data\\05-versions-space.pdf';
  let dataBuffer = fs.readFileSync(PDF_FILE);
  Pdf(dataBuffer).then(function (data) {
    fs.writeFileSync("./test/data/05-versions-space.txt", data.text, {
      encoding: "utf8",
      flag: "w",
    });
  }).catch(function(err) {
    console.error(err);
  });
}
*/